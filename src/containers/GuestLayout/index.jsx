import React from "react";
import { Outlet } from "react-router";

const GuestLayout = () => {
    return (
        <div>
            sad
            <Outlet />
        </div>
    );
};

export default GuestLayout;
