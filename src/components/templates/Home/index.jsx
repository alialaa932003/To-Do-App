import React from "react";
// import { Menu } from "../../organisms";
// import { Menu } from "../../organisms/";
import { Menu } from "../../organisms";
const Home = (props) => {
    return (
        <div>
            <Menu text={props.text} />
        </div>
    );
};

export default Home;
